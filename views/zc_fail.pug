<!DOCTYPE html>
html(lang="en")
head
	meta(charset="UTF-8")
	title Document
body
	h1 用户名已被注册请从新注册
	script.
		var i=2;
		function show(){
			if(i<1){
				window.history.back(-1); 
			}
			else{
				i--
			}
		}
		setInterval(show,1000);